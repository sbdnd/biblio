{{ form_start(form) }}

    <div class="row">
        <div class="col-sm-6">{{ form_row(form.codeExemplaire, { 'label': 'Code exemplaire' }) }}</div>
        <div class="col-sm-6">{{ form_row(form.dateAcquisition, { 'label': "Date d'acquisition de l'exemplaire" }) }}</div>
        <div class="col-sm-3">{{ form_row(form.dispo, { 'label': 'Disponible' }) }}</div>
    </div>
    <div class="row">
        <div class="col-sm-6">{{ form_row(form.livre.titre, { 'label': 'Titre du livre' }) }}</div>
        <div class="col-sm-6">{{ form_row(form.livre.resume, { 'label': 'Résumé' }) }}</div>
    </div>
    <div class="row">
        <div class="col-sm-6">{{ form_row(form.livre.annee, { 'label': 'Parution' }) }}</div>
        <div class="col-sm-6">{{ form_row(form.livre.isbn) }}</div>
    </div>
    <div class="row">
        <div class="col-sm-6">{{ form_row(form.livre.auteurs) }}
        <a href="{{ path('admin_auteur_new') }}" type="button" class="btn btn-outline-primary btn-sm mb-4">L'auteur n'est pas encore dans la liste !!</a>
        </div>
        <div class="col-sm-6">{{ form_row(form.livre.editeur) }}
        <a href="{{ path('admin_editeur_new') }}" type="button" class="btn btn-outline-primary btn-sm mb-4">L'éditeur n'est pas encore dans la liste !!</a>
        </div>
    </div>
    <div class="row">
        <div class="col-sm-6">{{ form_row(form.livre.genre) }}
        <a href="{{ path('admin_genre_new') }}" type="button" class="btn btn-outline-primary btn-sm mb-4">Le genre n'est pas encore dans la liste !!</a>
        </div>
        <div class="col-sm-6">{{ form_row(form.livre.imageFile, { 'label': 'Insérer une image' }) }}</div>
    </div>

    {{ form_widget(form) }}
    <button class="btn btn-primary mb-4">{{ button_label|default('sauvegarder') }}</button>
{{ form_end(form) }}
