<form method="post" action="{{ path('admin_exemplaire_delete', {'id': exemplaire.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ exemplaire.id) }}">
    <button class="btn btn-danger mb-4"><i class="fa fa-trash"></i> supprimer</button>
</form>
