{% extends 'base.html.twig' %}

{% block title 'genre' %}

{% block body %}

    <div class="container">

        <div class=" bg-warning text-center text-white">
            <h3>Caractéristiques</h3>
        </div>

        <table class="table">
            <tbody>
                <tr>
                    <th>Id</th>
                    <td>{{ genre.id }}</td>
                </tr>
                <tr>
                    <th>Libelle</th>
                    <td>{{ genre.libelle }}</td>
                </tr>
            </tbody>
        </table>

        <a class="btn btn-primary mb-2" href="{{ path('admin_genre_edit', {'id': genre.id}) }}">modifier</a>
        <div>
            <a class="btn btn-warning" href="{{ path('admin_genre_index') }}">retour à la liste</a>
        </div>

        {{ include('admin/genre/_delete_form.html.twig') }}

    </div>

{% endblock %}
