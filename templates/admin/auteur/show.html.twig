{% extends 'base.html.twig' %}

{% block title 'auteur' %}

{% block body %}

    <div class="container">

        <div class=" bg-warning text-center text-white">
            <h3>Caractéristiques</h3>
        </div>

        <table class="table">
            <tbody>
                <tr>
                    <th>Id</th>
                    <td>{{ auteur.id }}</td>
                </tr>
                <tr>
                    <th>NomAuteur</th>
                    <td>{{ auteur.nomAuteur }}</td>
                </tr>
                <tr>
                    <th>PrenomAuteur</th>
                    <td>{{ auteur.prenomAuteur }}</td>
                </tr>
                <tr>
                    <th>Nationalite</th>
                    <td>{{ auteur.nationalite }}</td>
                </tr>
            </tbody>
        </table>

        <a class="btn btn-primary mb-2" href="{{ path('admin_auteur_edit', {'id': auteur.id}) }}">modifier</a>
        <div>
            <a class="btn btn-warning" href="{{ path('admin_auteur_index') }}">retour à la liste</a>
        </div>

        {{ include('admin/auteur/_delete_form.html.twig') }}

    </div>

{% endblock %}
